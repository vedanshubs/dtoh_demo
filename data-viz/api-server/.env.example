OPENAI_API_KEY=
MCP_SERVER_PATH=../mcp-server/server.py
# Must match CollectionOrder.AccountNumber in the MySQL DB (UBS001 for the demo DB).
# Leaving this as DEMO_CLIENT makes every analytics query return 0 rows.
DEMO_CLIENT_ID=UBS001
PORT=8006
