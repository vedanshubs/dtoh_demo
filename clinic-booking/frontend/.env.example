# Google Maps JavaScript API key — powers the clinic map in the booking UI.
# Create a key in Google Cloud Console (Maps JavaScript API + Geocoding API),
# restrict it by HTTP referrer to http://localhost:5173/* and set a daily quota.
# Without this, the map panel shows a "failed to load" error.
VITE_GOOGLE_MAPS_API_KEY=
